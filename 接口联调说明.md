# 车牌识别算法部署验证工具：接口联调说明


## 1、文档定位与信息分层

本文用于组织客户端、设备端、算法端和测试人员的接口确认与联调，不是已经生效的接口契约。当前 Web Demo 和旧 Qt 项目只能作为交互或历史实现参考，不能证明目标设备接口已经确认。

| 信息层级 | 含义 | 当前处理方式 |
| --- | --- | --- |
| 已确认产品规则 | 已由产品讨论确定，真实 MVP 必须遵守 | 直接写入本文，并与 PRD 保持一致 |
| 待联调接口事实 | 只有设备资料或真实联调能够确认 | 使用 `[待确认]`，不填写虚构值 |
| 历史代码参考 | 旧 Qt 项目曾使用过的路径、端口或处理方式 | 仅供排查来源，不作为新项目默认值 |
| 当前原型表现 | HTML Demo 使用模拟状态、定时器和本地数据 | 只能验证交互，不能作为接口已实现证据 |
| 真实运行结果 | 在指定设备、版本和环境下实际执行的结果 | 当前尚未产生；联调后按第 7 章记录 |

发生冲突时，业务规则以已评审的 `PRD.md` 为准；接口字段、端口和协议以双方签字或确认的联调定版记录为准。任何联调结论如改变业务规则、范围或 P0 验收条件，必须先回写 PRD。

## 2、已确认的接口相关产品规则

这些规则不是接口已经实现的证明，而是联调方案必须满足的产品边界。

1. 相机端 XML 是相机配置的事实来源；PC 本机 IPv4 不属于相机配置，不写入 XML。
2. 配置修改流程为：读取相机当前 XML、修改本地副本中的目标字段、上传、重新从相机读取、按字段语义比较。目标字段一致才算配置生效。
3. 不引入配置版本号或 PC 端并发冲突管理。每次修改前后都以相机实际读取结果为准。
4. 抓拍开始、停止和有效触发计数以设备确认结果为准，不能以用户点击按钮或客户端收到请求代替设备事实。
5. 抓拍中锁定触发方式、线圈、上限、车道、相机和视频等会影响结果的配置。
6. 同一当前验证任务可以多次开始和停止抓拍；每次开始重新计算当次抓拍数量，已有记录继续保留。
7. 结果按设备一次性回传处理。首期不设计业务层结果重试、设备补发、客户端主动拉取图片或自动补偿流程。
8. 意外收到相同结果时，客户端应使用稳定归属和去重信息更新同一记录，不得新增重复记录。具体标识字段和组合规则待联调确认。
9. 设备按单条顺序处理时，上一条处理结束后才进入下一条；不设计“后一条已经到达、前一条仍在等待”的并行结果状态。
10. 已建立可归属记录但尚无照片时显示等待；设备处理结束或确认等待期结束仍无任何照片时，才进入系统异常。该异常不停止当前抓拍。
11. 收到任一照片后不得自动归为系统异常；即使算法处理失败，仍由用户判定正确或错误。
12. 等待照片和待确认是处理中状态，不设置为结果统计项；最终结果只统计正确、识别错误和系统异常。
13. 不允许删除单条记录；抓拍停止后，用户可以二次确认清空整个当前验证任务。

## 3、P0 接口能力与待确认项

以下表格描述真实 MVP 必须获得的能力和成功证据。字段名称、协议和参数未确认时不预先指定。

| 接口阶段 | 最小业务能力 | 成功证据 | 失败时的产品处理 | 待联调确认 |
| --- | --- | --- | --- | --- |
| 设备连接 | 客户端能够与目标边缘设备建立受控连接并识别连接结果 | 设备返回或可查询的连接成功事实 | 配置区和进入实时验证入口保持不可用 | 协议、地址、端口、认证、权限、超时、错误码 |
| XML 读取 | 从相机获取当前完整配置并解析目标字段 | 原始 XML、读取时间、目标字段和页面回显一致 | 标明读取失败，不用本地旧值冒充设备当前值 | 实际路径、编码、传输方式、节点映射、缺失字段处理 |
| XML 上传与回读 | 上传修改后的 XML，并重新读取相机当前配置 | 提交值、回读值和语义比较结果可核对 | 回读失败或不一致时不显示生效、不建立新配置段 | 上传方式、权限、应用时机、是否重启、语义等价规则 |
| 算法部署 | 将合法算法文件交给设备加载，并获得可验证结果 | 文件校验、设备加载状态或其他定版成功证据 | 保持算法未就绪，不允许以按钮点击作为成功 | 文件格式、目标位置、校验、加载、切换、重启、回滚 |
| 视频预览 | 获取目标视频并判断首帧、播放和断流状态 | 页面可播放画面和可核对的视频状态 | 视频项标记未就绪，阻止进入实时验证 | RTSP 或其他协议、地址结构、认证、超时、断流检测 |
| 抓拍开始 | 向设备发出开始请求并查询或接收设备确认 | 设备确认已进入抓拍运行状态 | 返回未开始或显示状态待确认，禁止重复开始 | 协议、方法、字段、确认方式、超时、错误码、幂等要求 |
| 抓拍停止 | 向设备发出停止请求并查询或接收设备确认 | 设备确认已经停止 | 显示停止确认中或状态待确认，不直接重复发送停止命令 | 确认方式、超时、状态查询、关闭页面时的处理 |
| 有效触发计数 | 获取设备确认的有效触发数量 | 计数能够与设备证据核对 | 不使用页面点击数或回调请求数冒充有效触发数 | 计数来源、更新频率、上限停止证据 |
| 结果回传 | 将可归属的算法结果和照片一次性发送给客户端 | 原始结果、照片引用、接收时间和归属信息完整 | 无法归属的不进入正式记录；不启动补发或主动拉图 | 监听方式、路径、认证、字段、图片方式、大小限制、错误码 |
| 处理结束 | 对无照片记录提供可判断的结束事实，或允许客户端按定版等待期结束 | 结束信号或等待期时间线可核对 | 结束前保持等待；结束后仍无照片才记系统异常 | 结束信号、等待期起点、时长、优先级、是否可配置 |
| 本地保存 | 保存当前任务、配置段、记录、证据和人工判定，并能恢复最近有效状态 | 保存结果、恢复结果和数据校验一致 | 保存失败不得覆盖最近有效状态 | 存储技术、数据结构、目录、原子保护、容量、清理方式 |

## 4、配置 XML 联调流程

### 4.1 正常流程

1. 客户端连接目标设备。
2. 客户端从待确认路径读取相机当前 XML。
3. 客户端解析并回显需要展示的目标字段。
4. 用户修改允许修改的字段；客户端只修改本地 XML 副本中的对应节点。
5. 客户端校验字段类型、范围、枚举和线圈坐标。
6. 客户端上传修改后的完整 XML 或设备接口要求的内容。
7. 客户端重新从相机读取当前 XML。
8. 客户端按照定版字段语义比较提交值与回读值。
9. 一致时显示配置生效并建立新配置段；不一致时显示未确认生效，并保留提交值、回读值和时间用于排查。

### 4.2 失败与边界

- 读取失败：不展示旧本地值为相机当前值。
- 上传失败：不进入回读成功状态，不自动重复上传。
- 回读失败：不显示配置已经生效。
- 回读不一致：以相机回读值为事实，不建立新配置段。
- XML 节点顺序、空白和格式差异不直接判失败，应比较定版目标字段的语义。
- 具体失败码、是否允许用户重新提交以及设备应用配置的时机，由联调定版；不得在未确认前写成自动重试机制。

## 5、结果回传与记录归属

### 5.1 最小业务信息

每条结果至少需要支持以下业务语义，具体字段名称、类型、是否嵌套和生成方均待联调确认：

| 业务语义 | 用途 | 当前状态 |
| --- | --- | --- |
| 设备归属信息 | 判断结果来自哪台目标设备 | `[待确认字段]` |
| 当前验证任务归属信息 | 防止结果进入错误任务 | `[待确认字段及下发/回传方式]` |
| 稳定记录唯一信息 | 归并同一记录并处理意外重复回传 | `[待确认字段及唯一性范围]` |
| 结果时间 | 还原处理时间线 | `[待确认设备时间或客户端接收时间口径]` |
| 算法原始输出 | 保存算法实际返回事实 | `[待确认字段]` |
| 照片信息 | 关联车辆全景图或车牌小图 | `[待确认编码、文件或引用方式]` |
| 处理结束事实 | 无照片时判断何时结束等待 | `[待确认信号或等待期规则]` |
| 配置关联信息 | 关联记录实际使用的配置段 | `[待确认直接字段或客户端时间关联方式]` |



### 5.2 一次性回传规则

- 首期按一次性结果回传设计。
- 设备不进行业务层补发，客户端不主动向设备拉取缺失照片。
- 客户端不因为无照片、算法失败或等待超时而要求设备重传结果。
- 如果网络或底层协议自身存在传输保障，由研发在技术设计中说明，但不能把它描述成新的业务补偿流程。
- 意外重复到达的相同结果只做幂等归并，不把“兼容重复”解释为产品主动重试。

### 5.3 记录状态与统计

| 条件 | 记录状态 | 是否进入最终结果统计 | 后续处理 |
| --- | --- | --- | --- |
| 已建立可归属记录，尚无任何照片，处理未结束 | 等待照片 | 否 | 继续等待，不停止当前抓拍 |
| 已收到至少一张照片，尚未人工判定 | 待确认 | 否 | 用户判定正确或错误 |
| 有照片且人工判定正确 | 正确 | 是 | 计入正确 |
| 有照片且人工判定错误 | 识别错误 | 是 | 计入识别错误 |
| 处理结束或确认等待期结束仍无任何照片 | 系统异常 | 是 | 计入系统异常，当前抓拍继续 |

已完成总计的固定口径为：

```text
已完成总计 = 正确数量 + 识别错误数量 + 系统异常数量
```

等待照片和待确认只在记录详情或列表中显示，不设置为结果统计项。

## 6、本地运行、存储和安全边界

| 事项 | 已确认边界 | 尚待确认 |
| --- | --- | --- |
| 目标环境 | 首期目标为 Windows 11 24H2 64 位 | 目标硬件、安装权限、网络策略和离线依赖 |
| 数据保存 | 必须支持自动保存、恢复最近有效状态和失败时不覆盖旧快照 | SQLite、其他数据库或文件方案，具体表结构和事务方式 |
| 图片保存 | 图片必须与记录稳定关联，不以完整车牌号作为目录名或文件名 | 根目录、分层方式、容量、清理和备份策略 |
| 凭据保护 | 不得在页面、日志、PRD 或截图中保存明文凭据 | Windows 凭据管理器或其他安全方案、访问权限和轮换方式 |
| 清理 | 不允许删除单条记录；二次确认后清空当前任务业务数据和恢复快照 | 文件删除顺序、失败回滚、清理证明和敏感数据残留检查 |

当前尚未选定 SQLite、固定本地目录、凭据管理工具或数据库表结构。任何候选技术必须进入客户端技术设计并经过评审，不能因为旧代码或旧草案曾提到就视为已决定。

## 7、联调执行记录

每个接口使用一行或一组记录填写，未执行时必须标记“未执行”。

| 记录项 | 填写要求 |
| --- | --- |
| 联调环境 | 客户端版本、设备型号、固件和算法版本、网络、日期和参与方 |
| 接口定义 | 协议、方向、地址或路径、字段、认证、超时、错误码和成功证据 |
| 测试输入 | 使用脱敏样例或受控测试数据，不记录真实凭据 |
| 实际输出 | 保存脱敏响应、设备状态、时间线和截图位置 |
| 实际结果 | 通过、失败、部分通过或未执行，不用计划代替结果 |
| 差异与缺陷 | 预期、实际表现、影响、责任方和缺陷编号 |
| 定版结论 | 已确认值、适用设备版本、限制及对应 PRD/验收用例变更 |

当前真未执行目标设备联调，暂无接口通过结论。

## 8、P0 联调检查清单

| 编号 | 检查场景 | 预期结果 | 当前结果 |
| --- | --- | --- | --- |
| INT-01 | 连接设备并读取 XML | 页面字段与脱敏回读内容一致 | 未执行 |
| INT-02 | 修改一个合法 XML 字段并上传、回读 | 语义一致才显示生效并建立配置段 | 未执行 |
| INT-03 | 设备返回上传成功，但重新读取的目标字段与提交值不一致 | 显示未确认生效，不建立配置段 | 未执行 |
| INT-04 | 播放视频并中断视频源 | 正确显示首帧、播放和断流状态 | 未执行 |
| INT-05 | 发出开始请求并获得设备确认 | 确认前显示启动中，确认后进入抓拍中 | 未执行 |
| INT-06 | 发出停止请求并获得设备确认 | 确认前不提前显示已停止 | 未执行 |
| INT-07 | 达到 1—100 范围内设置的上限 | 设备有效触发数达到上限后停止当前抓拍 | 未执行 |
| INT-08 | 回传有照片的正常或算法失败结果 | 形成待确认记录，不进入最终结果统计 | 未执行 |
| INT-09 | 回传已归属但最终无任何照片的结果 | 结束前保持等待，结束后记系统异常，抓拍继续 | 未执行 |
| INT-10 | 意外重复投递同一结果 | 不新增记录、不重复统计 | 未执行 |
| INT-11 | 停止抓拍时仍有一条在途记录 | 停止新增抓拍；在途记录按结束信号或等待期继续处理 | 未执行 |
| INT-12 | 自动保存后异常退出并恢复 | 恢复最近有效当前任务且汇总重算一致 | 未执行 |
| INT-13 | 二次确认清空当前任务 | 完整清理任务数据和恢复快照，不提供单条删除 | 未执行 |

## 9、联调输出与变更控制

联调完成后至少输出：

- 已确认接口说明；
- XML 节点与页面字段映射；
- 配置语义等价规则；
- 脱敏请求、响应和结果样例；
- 成功证据、错误码和状态查询规则；
- 记录归属与意外重复结果去重规则；
- 确认等待期和处理结束信号；
- 设备、固件和算法适用版本范围；
- 联调执行记录、缺陷和结论；
- 受影响的 PRD、原型和 P0 验收用例变更清单。

未确认内容不得写入“已实现能力”或交付结论。若目标设备能力无法满足 P0 规则，应先评审设备端改造、产品范围调整或记录归属方案，不能在文档中用未经确认的字段、端口、重试或补偿机制掩盖缺口。
